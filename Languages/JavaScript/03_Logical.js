// Logical Operators

// && (AND)
// || (OR)
// ! (NOT) or reverse the value

// AND
let isLoggedin = true;
let isVerified = false;

console.log(isLoggedin && isVerified); // false

// OR
let isEmailuser = true;
let isGoogleuser = false;

console.log(isEmailuser || isGoogleuser); // true
